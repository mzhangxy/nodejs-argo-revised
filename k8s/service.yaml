apiVersion: v1
kind: Service
metadata:
  name: trial-service
  namespace: default
  labels:
    app: trial
spec:
  selector:
    app: trial
  ports:
  - port: 3000
    targetPort: 8080
  type: LoadBalancer  # 暂时用 ClusterIP，后续可加 Ingress 暴露
